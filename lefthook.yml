pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,js,json}"
      run: pnpm biome check --no-errors-on-unmatched {staged_files}
    - name: typecheck
      run: pnpm tsc --noEmit
