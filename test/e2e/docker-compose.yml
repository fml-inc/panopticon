services:
  test-sync-validation:
    build:
      context: ../..
      dockerfile: test/e2e/Dockerfile
      target: test-sync-validation
    environment:
      - ANTHROPIC_API_KEY
      - OPENAI_API_KEY
      - GEMINI_API_KEY
      - SHELL=/bin/bash
      - PANOPTICON_PORT=4318
